#include<stdio.h>

int main()
{

}

int solution(int K, int L, int M, int N, int P){

	long long area1 = (M - K) * (N - L);
	long long area2 = (R - P) * (S - Q);

	long long sum = 0;

	int kp,lq,mr,ns,intersect;

	sum = area1 + area2;

	if (M < P || R < K || S < L || N < P){
		;
	}else{
		kp = K > P ? K : P;
		lq = L > Q ? L : Q; 
		s=s1(xx1,yy1,xx2,yy2);  
	}
}